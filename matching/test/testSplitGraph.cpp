#include "testConfig.h"
#include "utility/splitGraph/SplitGraph.h"

TEST(SplitGraphTestSuite, splitBasic) {
    // Graph* query_graph = new Graph(true);
    // query_graph->loadGraphFromFile(TEST_QUERY);
    // query_graph->buildCoreTable();

    // query_graph->printGraphMetaData();
    // query_graph->printGraph();


    // SplitGraph* multigraphs = new SplitGraph(query_graph, SplitPattern::HYBRIDPATH_RANDOM, SPLITMODE::Q);

    // std::cout << multigraphs->getSubgraphCount() << std::endl;

    // delete multigraphs;
    // delete query_graph;


}

TEST(UnitEnumTestSuite, unitEnumBasic) {
    // Graph* query_graph = new Graph(true);
    // query_graph->loadGraphFromFile(TEST_QUERY);
    // query_graph->buildCoreTable();

    // SplitGraph* multigraphs = new SplitGraph(query_graph, SplitPattern::HYBRIDPATH_RANDOM, SPLITMODE::Q);    
    // delete query_graph;
}
/**
    1. test split graph     Over
    2. test unit enum       
    3. test concatenate
    
    */

